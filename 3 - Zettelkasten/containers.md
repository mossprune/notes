#computer-science #cloud-native #containers

Containers provide lots of advantages. The slogan for Docker is "build once, run everywhere". > Containers bundle an application together with everything it needs to run (dependancies). 

This package is isolated from the computer it runs on. This package is called the "image", images run the same regardless of where it runs.

